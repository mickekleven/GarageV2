@model Vehicle
@using GarageV2.Utilities
@using static GarageV2.Utilities.Enums


<div class="col-md-4">

@*    <select class="form-select form-select-sm" name="selectedValue" onchange="vehicleTypeFunc(this.value);" id="selvehicletype">

        @foreach (var ty in Enum.GetValues(typeof(VehicleType)))
        {
            <option class="vihtype" value="@ty">@ty</option>
        }
    </select>*@



@*    @Html.DropDownListFor(model => 
        model.VehicleType, 
        Enum.GetValues(typeof(VehicleType)),"key")
)*@



    <select class="form-select form-select-sm" name="selectedValue" onchange="vehicleTypeFunc(this.value);" id="selvehicletype">

        @foreach (var ty in Enum.GetValues(typeof(VehicleType)))
        {
            <option class="vihtype" value="@ty">@ty</option>
        }
    </select>

</div>

