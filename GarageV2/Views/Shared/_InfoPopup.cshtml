@model VehicleViewModel




@if (ViewData["UserMessage"] is not null)
{
    <div class="row" id="popup-er">
        <div class="col-md-12">
            <div class="popup-main">
                <div class="popup-inner">
                    <div class="popup-body bg-light">

                        <h2>MEDDELANDE @DateTime.Now.ToString("hh:mm")</h2>

                        @if (ViewData["UserMessage"] is not null)
                        {
                            <p>@ViewData["UserMessage"]</p>
                        }


                        <p>Detta är paragraph texten</p>
                    </div>

                    <div class="popup-footer">

                        <div class="popup-footer_ch1">
                            <button class="btn btn-outline-primary">OK</button>
                        </div>

                        <div class="popup-footer_ch2">
                            <button class="btn btn-outline-primary">OK</button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
}










